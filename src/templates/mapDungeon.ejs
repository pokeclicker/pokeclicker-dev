<% /* %>

Parameters:
    - name : Required : String
        The name of the Dungeon to be added.
        Should match what is used in the DungeonList.

    - moveToTown : Optional : String
        The name of a town to move to when this element is clicked.
        Used when the dungeon is accessed via a town.

    - x : Required : Number
        x axis position of the dungeon icon.

    - y : Required : Number
        y axis position of the dungeon icon.

<% */ %>

<rect fill="#ffffff" height="15" stroke="none" width="15"
    x="<%= x %>" y="<%= y %>">
</rect>
<rect data-bind="
    click:function(){MapHelper.moveToTown('<%= (locals.moveToTown) ? locals.moveToTown : name %>')},
    attr: { class: MapHelper.calculateTownCssClass('<%= name %>') }"
    class="city" data-town="<%= name %>"
    fill="url(#mx-gradient-dae8fc-1-7ea6e0-1-s-0)"
    height="12" stroke="#6c8ebf" width="12" x="<%= (x + 1.5) %>" y="<%= (y + 1.5) %>">
</rect>